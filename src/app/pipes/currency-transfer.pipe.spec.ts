import { CurrencyTransferPipe } from './currency-transfer.pipe';

describe('CurrencyTransferPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyTransferPipe();
    expect(pipe).toBeTruthy();
  });
});
